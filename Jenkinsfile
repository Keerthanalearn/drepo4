pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Hello Build'
            }
        }
      stage('Deploy') {
            steps {
                echo 'Hello World'
            }
        }
    }
}
